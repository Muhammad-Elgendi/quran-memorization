from setuptools import find_packages, setup

setup(
    name="quran-memorization-backend",
    version="1.0.0",
    packages=find_packages(),
    include_package_data=True,
    python_requires=">=3.11",
)
